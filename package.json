{
  "name": "redux-rx-http",
  "version": "0.5.0",
  "description": "Uses react-observable to provide a nice way of talking to an API.",
  "main": "index.js",
  "repository": "git@github.com:radiosilence/react-observable-http.git",
  "author": "James Cleveland",
  "license": "MIT",
  "devDependencies": {
    "source-map-loader": "^0.2.1",
    "tslint": "^5.3.2",
    "tslint-config-airbnb": "^5.0.1",
    "tslint-react": "^3.0.0",
    "tsutils": "^2.1.0",
    "typescript": "^2.3.3"
  },
  "dependencies": {
    "@types/jest": "^19.2.4",
    "@types/node": "^7.0.29",
    "redux": "^3.6.0",
    "redux-observable": "^0.14.1",
    "rxjs": "^5.4.0"
  },
  "scripts": {
    "start": "npm run tsfmt:replace && npm run dev:build",
    "tsc:compile": "tsc",
    "tsc:watch": "tsc --watch --pretty",
    "lint": "tslint ./src/**/*.{ts,tsx}",
    "prepublish": "rm -rf dist; npm run tsc:compile",
    "lint:fix": "tslint --fix ./src/**/*.{ts,tsx}"
  }
}
